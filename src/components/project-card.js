import React from "react";
import { Card, Badge, Button } from "react-bootstrap";

export function projectCard({data, setOrdered}){
    return(
        <Card className="h-100 shadow-sm bg-white rounded"></Card>
    )
}